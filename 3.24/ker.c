
char s[N], t[N];
void ClearScreen();  // 

void puts(char *s, int len, int r, int c); // print a string at the (r,c), used for terminal 
void putchar(char c, int r, int c);  // used for 

void shell();


char getchar();

void dir();
